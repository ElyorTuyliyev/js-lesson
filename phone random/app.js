var number = prompt(`Enter your number`);
var phoneList = new Set();
for (var i = 1; i <= number; i++) {
  var fullPhone = `+99891`;
  for (var phoneList = 0; phoneList < 7; phoneList++) {
    fullPhone += Math.floor(Math.random);
  }
  console.log();
}
